import { NativeType } from "./utils";
/**
 * RTID stands for RunTime ID.
 * It is an identifier that is guaranteed to be unique for each entity in the scene.
 * It is only valid for the lifetime of the session.
 *
 * @category Engine Types
 */
export type RTID = NativeType<bigint>;
